---
import { SITE } from '~/config.mjs';

import Layout from '~/layouts/PageLayout.astro';
import Hero from '~/components/widgets/Hero.astro';
import VideoSection from '~/components/widgets/VideoSection.astro';
import PostCard from '~/components/widgets/PostCard.astro';
import blogPosts from '~/blogdata/blogposts.json';
import SignUpForm from '~/components/widgets/SignUpForm.astro';
import ComingSoon from '~/components/widgets/ComingSoon.astro';
import LandingDashboard from '~/components/widgets/LandingDashboard.astro';
import HIVEThoughts from '~/components/widgets/HIVEThoughts.astro';
import Benefits from '~/components/widgets/Benefits.astro';
import MarketCard from '~/components/widgets/MarketCard.astro';

const meta = {
  title: SITE.title,
  description: SITE.description,
  dontUseTitleTemplate: true,
};

---


<Layout {meta}>
  <section class="relative duration-300 ease-out obsSection opacity-0 translate-y-10">
    
    <Hero>
      <Fragment slot="title">
        Trading is Evolving<br>And the <span class="text-transparent bg-clip-text bg-gradient-to-r from-gradient via-bluegradient to-gradient animate-linear bg-[length:200%_auto]">Machines are Winning</span>
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-gradient via-bluegradient to-gradient animate-linear bg-[length:200%_auto] absolute blur-xl opacity-75 -z-20 bottom-4 right-0">Machines are Winning</span>
      </Fragment>
      <Fragment slot="subtitle">
        We recognize machines are better at trade decision making than humans
      </Fragment>
    </Hero>
    <LandingDashboard />
    <div class="absolute -top-72 left-1/2 transform -translate-x-1/2">
      <div class="rounded-full w-96 h-96 bg-accent blur-3xl opacity-20 scale-125"></div>
    </div>
  </section>

  <HIVEThoughts />
  <MarketCard />

  <VideoSection
    title="How it works";
    content="As markets evolve and the role of technology in trading becomes more pronounced, FinGen provides a crucial advantage. Our AI-driven solutions, combined with unmatched efficiency and flexibility, ensure you're equipped to navigate the complexities of the modern trading environment.";
  ></VideoSection>

  <Benefits />
  
  <ComingSoon />

<div class="w-full overflow-x-clip duration-300 ease-out obsSection opacity-0 translate-y-10">
  <section class="px-6 mx-auto max-w-6xl lg:px-8 py-20 relative">
    <div class="pb-8 inline-flex justify-between w-full items-center">
      <h2 class="text-justify text-3xl md:text-4xl font-bold tracking-tight text-transparent bg-clip-text bg-gradient-to-b from-[#e4e5e7] to-[#7b7b7e]">Latest articles</h2>
      <a class="bg-primary text-textcolor border border-secondary hover:bg-secondary hover:text-textdark transition-all px-6 py-2 rounded-full" href="/blog">See All</a>
    </div>
    
    <div class="w-96 h-96 rounded-full bg-gradient blur-3xl opacity-20 absolute top-1/2 -translate-y-1/2 -left-80 scale-150 -z-10"></div>
    <div class="w-96 h-96 rounded-full bg-bluegradient blur-3xl opacity-20 absolute top-1/2 -translate-y-1/2 -right-80 scale-150 -z-10"></div>

    <div class="flex flex-col md:grid md:grid-cols-3 gap-4 md:gap-8">
      {blogPosts.slice(0,3).map((post) => (
        <PostCard
          title={post.title}
          date={post.date}
          tag={post.tag}
          image={post.image}
          url={`/blog/${post.slug}`}
        />
      ))}
    </div>
  </section>
</div>
  
  <SignUpForm />
</Layout>